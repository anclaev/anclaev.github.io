import { StyledMenuIcon, StyledMenuWrapper } from '../../styled/components'

const MenuIcon: React.FC = () => (
  <StyledMenuWrapper className="cursor">
    <StyledMenuIcon className="cursor">
      <span className="cursor"></span>
      <span className="cursor"></span>
      <span className="cursor"></span>
    </StyledMenuIcon>
  </StyledMenuWrapper>
)

export default MenuIcon
